project_name = "replace-with-project-name"
environment  = "dev"
network_name = "rag-platform-dev-network"
network_cidr = "10.10.0.0/16"

# compute_nodes and future_compute_autoscaling have safe, provider-neutral
# defaults in variables.tf. Override only after architecture approval.
# storage_definitions have provider-neutral defaults in variables.tf.
# security_configuration has provider-neutral defaults in variables.tf.
# kubernetes_bootstrap_configuration is inert and has defaults in variables.tf.
# vault_configuration contains reference-only defaults in variables.tf.
# observability_configuration contains disabled, reference-only defaults in variables.tf.
